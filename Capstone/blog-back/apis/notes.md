##Cross origin vs same origin

Cross origin request means when the cleint and server application running in different domains
same origin request means  cleint also running on the same domain of the server
cookies will send along with request automatically in same origin request.but for cross origin request the tokebn should be explicitly included to the request 